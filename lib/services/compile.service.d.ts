import { SymlType } from "../types/syntax";
import { ObjectOf } from "../types/common.types";
export declare class CompileService {
    static compileSaml(clientData: ObjectOf<any>, types?: ObjectOf<SymlType>): ObjectOf<any>;
}
